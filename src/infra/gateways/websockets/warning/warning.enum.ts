
export enum WARNINGS_EVENTS {
    CONNECT = 'connect',
    DISCONNECT = 'disconnect',
}

export enum WARNINGS {
    NAMESPACE = 'warnings',
    "CREATED_WARNINGS" = 'created_warnings',
    "UPDATED_WARNINGS" = 'updated_warnings',
}